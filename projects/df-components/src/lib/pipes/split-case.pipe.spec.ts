import { SplitCasePipe } from './split-case.pipe';

describe('SplitCasePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitCasePipe();
    expect(pipe).toBeTruthy();
  });
});
